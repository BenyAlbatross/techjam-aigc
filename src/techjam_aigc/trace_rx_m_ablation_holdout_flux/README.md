# Full TRACE-RX-M / FLUX holdout

Frozen snapshot of the current TRACE-RX-M training implementation. The complete `flux_1_schnell` family is excluded from S4 training and evaluated on validation only. The held-out gate is report-only.
