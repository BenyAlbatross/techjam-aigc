# BCE-only TRACE-RX-M / Gemini holdout

The pAUC and DDA-pair weights are zero. Architecture, data, augmentation, schedule, LoRA, and Gemini holdout match the full reference. The held-out gate is report-only.
